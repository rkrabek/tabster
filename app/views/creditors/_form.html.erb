<%= form_for @creditor do |f| %>
  <% if @creditor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creditor.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @creditor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>

<%= form_for @creditor do |f| %>
	<div>
	<%= f.label :name %>
	<%= f.text_field :name %>
	</div>
	
	<%= f.submit %>
<% end %>